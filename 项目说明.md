# AI Desktop Pet 项目说明文档

## 📋 项目概述

**AI Desktop Pet** 是一个基于 Tauri + React + TypeScript 构建的智能桌面宠物应用，集成了 AI 对话、Live2D 动画、语音交互、任务调度等多项功能，旨在打造一个能够"成长"的 AI 伴侣。

### 核心特性

- 🎨 **Live2D 动画渲染** - 支持自定义皮肤，实时表情切换
- 🤖 **AI 智能对话** - 集成多种 LLM 提供商（OpenAI、Anthropic、Ollama）
- 💭 **长期记忆系统** - 用户画像记录、情绪检测、上下文记忆
- 🫶 **主动关怀系统** - 偏好记忆、习惯追踪、智能提醒
- 🎙️ **语音交互** - 支持语音输入（STT）和语音输出（TTS）
- 📅 **任务调度系统** - 支持定时任务、间隔任务、事件触发任务
- 🔧 **Agent 工具系统** - 内置多种工具（文件操作、剪贴板、搜索、天气等）
- 🎮 **宠物养成系统** - 心情、精力、亲密度等属性管理
- 📊 **统计与成就** - 互动统计、成就解锁系统
- ⌨️ **快捷键系统** - 全局快捷键支持
- 📝 **Markdown 渲染** - 聊天消息支持 Markdown 格式
- 💾 **数据管理** - 数据导入/导出/备份/恢复功能
- 🔌 **MCP 协议支持** - Model Context Protocol 集成

---

## 🏗️ 技术架构

### 技术栈

#### 前端层
- **框架**: React 19.1.0 + TypeScript 5.8.3
- **构建工具**: Vite 7.0.4
- **状态管理**: Zustand 5.0.9
- **UI 组件**: Radix UI + Tailwind CSS
- **动画渲染**: oh-my-live2d 0.19.3 + PixiJS 8.14.3

#### 桌面端
- **框架**: Tauri 2.x
- **语言**: Rust
- **数据库**: SQLite (via @tauri-apps/plugin-sql)

#### AI 层
- **LLM SDK**: @ai-sdk/anthropic, @ai-sdk/openai, ollama-ai-provider
- **Agent 框架**: @langchain/langgraph 1.0.4
- **MCP**: @modelcontextprotocol/sdk 1.24.3

### 架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                        用户界面层                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ PetContainer │  │  ChatWindow  │  │SettingsWindow│     │
│  │  (Live2D)    │  │   (对话界面) │  │   (设置面板) │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                        状态管理层 (Zustand)                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │petStore  │ │chatStore │ │configStore│ │agentStore│      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │statusStore│ │skinStore │ │scheduler │ │careStore │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                   │
│  │assistant │ │toastStore│ │contextMenu│                   │
│  └──────────┘ └──────────┘ └──────────┘                   │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                        业务服务层                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │Live2D    │ │LLM Chat  │ │Voice     │ │Scheduler │      │
│  │Manager   │ │Service   │ │Manager   │ │Manager   │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │Agent     │ │Pet Status│ │Skin      │ │Statistics│      │
│  │Runtime   │ │Service   │ │Manager   │ │Service   │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │Memory    │ │Keyboard  │ │Data      │ │Assistant │      │
│  │Service   │ │Shortcuts │ │Manager   │ │Service   │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                        数据持久层                            │
│  ┌──────────────────────────────────────────────────┐      │
│  │              SQLite Database (pet.db)            │      │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐         │      │
│  │  │config    │ │pet_status│ │skins     │         │      │
│  │  └──────────┘ └──────────┘ └──────────┘         │      │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐         │      │
│  │  │tasks     │ │statistics│ │achievements│       │      │
│  │  └──────────┘ └──────────┘ └──────────┘         │      │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐         │      │
│  │  │memories  │ │user_     │ │agent_    │         │      │
│  │  │          │ │preferences│ │audit     │         │      │
│  │  └──────────┘ └──────────┘ └──────────┘         │      │
│  └──────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                        原生系统层 (Tauri)                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │Window    │ │File      │ │Clipboard │ │Shell     │      │
│  │Manager   │ │System    │ │Manager   │ │Executor  │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 项目结构

```
ai-desktop-pet/
├── src/                          # React 前端源码
│   ├── components/               # UI 组件
│   │   ├── pet/                  # 宠物相关组件
│   │   │   ├── PetContainer.tsx  # 宠物容器（Live2D 渲染）
│   │   │   ├── StatusBar.tsx     # 状态条（心情、精力、亲密度）
│   │   │   ├── InteractionPanel.tsx  # 互动面板
│   │   │   ├── ContextMenu.tsx   # 右键菜单
│   │   │   └── ...
│   │   ├── chat/                 # 对话相关组件
│   │   │   ├── ChatWindow.tsx    # 对话窗口
│   │   │   ├── MessageList.tsx   # 消息列表
│   │   │   ├── MessageItem.tsx   # 消息项（支持Markdown）
│   │   │   ├── InputArea.tsx     # 输入区域
│   │   │   └── ...
│   │   ├── settings/             # 设置相关组件
│   │   │   ├── GameSettingsWindow.tsx  # 设置主窗口
│   │   │   ├── Live2DSettings.tsx      # Live2D 设置
│   │   │   ├── SchedulerTestPanel.tsx  # 调度器测试面板
│   │   │   ├── AgentAuditPanel.tsx     # Agent 审计面板
│   │   │   ├── DataSettings.tsx        # 数据管理
│   │   │   ├── tabs/                   # 设置标签页
│   │   │   │   ├── AssistantTab.tsx    # AI助手设置
│   │   │   │   ├── BehaviorTab.tsx     # 行为设置
│   │   │   │   ├── AppearanceTab.tsx   # 外观设置
│   │   │   │   ├── PerformanceTab.tsx  # 性能设置
│   │   │   │   └── ...
│   │   │   └── ...
│   │   ├── agent/                # Agent 相关组件
│   │   ├── toast/                # Toast 通知组件 (Radix UI)
│   │   └── ui/                   # 通用 UI 组件 (shadcn/ui)
│   │
│   ├── stores/                   # Zustand 状态管理
│   │   ├── petStore.ts           # 宠物状态（位置、大小、可见性）
│   │   ├── petStatusStore.ts     # 宠物属性（心情、精力、亲密度）
│   │   ├── chatStore.ts          # 对话状态
│   │   ├── configStore.ts        # 配置状态
│   │   ├── agentStore.ts         # Agent 状态
│   │   ├── schedulerStore.ts     # 调度器状态
│   │   ├── skinStore.ts          # 皮肤状态
│   │   ├── careStore.ts          # 养成状态
│   │   ├── assistantStore.ts     # AI助手状态（偏好、习惯、提醒）
│   │   ├── toastStore.ts         # Toast 通知状态
│   │   ├── contextMenuStore.ts   # 右键菜单状态
│   │   └── ...
│   │
│   ├── services/                 # 业务服务
│   │   ├── live2d/               # Live2D 服务
│   │   │   ├── manager.ts        # Live2D 管理器
│   │   │   └── global-init.ts    # 全局初始化
│   │   ├── llm/                  # LLM 服务
│   │   │   ├── chat.ts           # 对话服务
│   │   │   ├── providers.ts      # 提供商配置
│   │   │   └── types.ts          # 类型定义
│   │   ├── agent/                # Agent 服务
│   │   │   ├── runtime.ts        # Agent 运行时
│   │   │   ├── base-tool.ts      # 工具基类
│   │   │   ├── tools/            # 内置工具
│   │   │   │   ├── clipboard.ts  # 剪贴板工具
│   │   │   │   ├── file.ts       # 文件工具
│   │   │   │   ├── search.ts     # 搜索工具
│   │   │   │   ├── weather.ts    # 天气工具
│   │   │   │   └── ...
│   │   │   └── workflows/        # 工作流
│   │   │       ├── graph.ts      # LangGraph 图
│   │   │       ├── supervisor.ts # 监督者
│   │   │       └── workers/      # 工作节点
│   │   ├── scheduler/            # 任务调度服务
│   │   │   ├── manager.ts        # 调度管理器
│   │   │   └── test.ts           # 测试工具
│   │   ├── pet/                  # 宠物服务
│   │   │   ├── status.ts         # 状态计算（衰减、边界检查）
│   │   │   ├── interaction.ts    # 互动处理
│   │   │   ├── growth.ts         # 成长阶段逻辑
│   │   │   ├── emotion.ts        # 表情映射
│   │   │   ├── expression-pack.ts # 表情包系统
│   │   │   ├── expression-packs/ # 表情包库
│   │   │   │   ├── default.ts    # 默认表情包
│   │   │   │   ├── qq.ts         # QQ风格表情包
│   │   │   │   └── ...
│   │   │   └── ...
│   │   ├── voice/                # 语音服务
│   │   │   ├── manager.ts        # 语音管理器
│   │   │   ├── tts-edge.ts       # Edge TTS
│   │   │   ├── tts-web.ts        # Web Speech TTS
│   │   │   └── stt-web.ts        # Web Speech STT
│   │   ├── skin/                 # 皮肤服务
│   │   │   ├── manager.ts        # 皮肤管理器
│   │   │   └── importer.ts       # 皮肤导入器
│   │   ├── database/             # 数据库服务
│   │   │   ├── index.ts          # 数据库初始化
│   │   │   ├── config.ts         # 配置表操作
│   │   │   ├── pet-status.ts     # 宠物状态表操作
│   │   │   ├── skins.ts          # 皮肤表操作
│   │   │   ├── tasks.ts          # 任务表操作
│   │   │   ├── statistics.ts     # 统计表操作
│   │   │   └── achievements.ts   # 成就表操作
│   │   ├── memory/               # 长期记忆服务
│   │   │   ├── index.ts          # 入口
│   │   │   ├── user-profile.ts   # 用户画像
│   │   │   ├── long-term-memory.ts # 长期记忆
│   │   │   └── emotion-detector.ts # 情绪检测
│   │   ├── keyboard/             # 快捷键服务
│   │   │   ├── index.ts          # 入口
│   │   │   └── shortcuts.ts      # 快捷键管理
│   │   ├── data/                 # 数据导入导出
│   │   │   ├── export.ts         # 数据导出
│   │   │   ├── import.ts         # 数据导入
│   │   │   ├── backup.ts         # 数据备份
│   │   │   └── validators.ts     # 数据验证
│   │   ├── statistics/           # 统计服务
│   │   ├── achievements/         # 成就服务
│   │   ├── mcp/                  # MCP 服务
│   │   └── weather/              # 天气服务
│   │
│   ├── hooks/                    # 自定义 Hooks
│   │   ├── useLive2D.ts          # Live2D Hook
│   │   ├── useChat.ts            # 对话 Hook
│   │   ├── usePetStatus.ts       # 宠物状态 Hook
│   │   ├── usePetActions.ts      # 宠物动作 Hook
│   │   ├── usePetCareLoop.ts     # 养成循环 Hook
│   │   ├── useVoice.ts           # 语音 Hook
│   │   ├── useDrag.ts            # 拖拽 Hook
│   │   ├── useWindowPlacement.ts # 窗口位置 Hook
│   │   └── ...
│   │
│   ├── types/                    # TypeScript 类型定义
│   │   ├── pet.ts                # 宠物类型
│   │   ├── config.ts             # 配置类型
│   │   ├── chat.ts               # 对话类型
│   │   ├── scheduler.ts          # 调度器类型
│   │   ├── agent.ts              # Agent 类型
│   │   └── ...
│   │
│   ├── styles/                   # 全局样式
│   │   ├── global.css            # 全局样式
│   │   └── design-tokens.css     # 设计令牌
│   │
│   ├── App.tsx                   # 主应用组件
│   ├── main.tsx                  # React 入口
│   ├── chat.tsx                  # 对话页面入口
│   └── settings.tsx              # 设置页面入口
│
├── src-tauri/                    # Tauri 原生端（Rust）
│   ├── src/                      # Rust 源码
│   │   ├── lib.rs                # 库入口
│   │   ├── commands.rs           # Tauri 命令
│   │   └── ...
│   ├── tauri.conf.json           # Tauri 配置
│   ├── Cargo.toml                # Rust 依赖配置
│   └── icons/                    # 应用图标
│
├── public/                       # 静态资源
│   ├── models/                   # Live2D 模型
│   │   ├── default/              # 默认模型
│   │   └── cat/                  # 猫模型
│   └── whitecatfree_vts/         # 白猫模型
│
├── docs/                         # 项目文档
│   ├── PRD-宠物养成系统.md       # 产品需求文档
│   ├── design/                   # 设计文档
│   ├── specs/                    # 规格文档
│   └── ...
│
├── scripts/                      # 工具脚本
│   ├── check-live2d.ts           # Live2D 检查脚本
│   ├── enable-live2d.ts          # Live2D 启用脚本
│   └── ...
│
├── package.json                  # 前端依赖配置
├── pnpm-lock.yaml                # pnpm 锁定文件
├── vite.config.ts                # Vite 配置
├── tsconfig.json                 # TypeScript 配置
├── tailwind.config.js            # Tailwind CSS 配置
└── README.md                     # 项目说明
```

---

## 🔄 核心业务流程

### 1. 应用启动流程

```
用户启动应用
    ↓
1. 初始化数据库 (initDatabase)
   ├─ 创建/连接 SQLite 数据库
   ├─ 执行数据库迁移
   └─ 初始化表结构
    ↓
2. 加载配置 (loadConfig)
   ├─ 从数据库读取配置
   ├─ 应用窗口设置（大小、位置、置顶）
   └─ 应用交互设置（点击穿透、记住位置）
    ↓
3. 加载宠物状态 (loadStatus)
   ├─ 读取上次保存的状态
   ├─ 计算时间衰减 (calculateDecay)
   ├─ 更新心情、精力值
   └─ 触发表情更新
    ↓
4. 加载皮肤 (loadSkins)
   ├─ 扫描可用皮肤
   ├─ 应用选中的皮肤
   └─ 初始化 Live2D 模型
    ↓
5. 初始化服务
   ├─ 统计服务 (initializeStatsService)
   ├─ 成就服务 (initializeAchievements)
   └─ 调度器 (scheduler.initialize)
    ↓
6. 渲染 UI
   ├─ 渲染 PetContainer (Live2D)
   ├─ 显示状态条
   └─ 显示欢迎气泡
    ↓
7. 监听事件
   ├─ 托盘菜单事件
   ├─ 调度器事件
   └─ 用户交互事件
```

### 2. 对话交互流程

```
用户发起对话
    ↓
1. 用户输入
   ├─ 文字输入（InputArea）
   └─ 语音输入（usePushToTalk）
       ├─ 录音
       ├─ STT 转文字
       └─ 填充到输入框
    ↓
2. 消息处理
   ├─ 添加用户消息到 chatStore
   ├─ 显示在 MessageList
   └─ 更新互动统计
    ↓
3. AI 响应生成
   ├─ 构建对话上下文
   ├─ 注入 System Prompt
   │   ├─ 基础人格设定
   │   ├─ 根据亲密度调整语气
   │   └─ 根据心情调整回复风格
   ├─ 调用 LLM API
   │   ├─ OpenAI
   │   ├─ Anthropic
   │   └─ Ollama
   └─ 流式返回响应
    ↓
4. 响应展示
   ├─ 添加 AI 消息到 chatStore
   ├─ 显示在 MessageList
   ├─ TTS 语音播放（可选）
   └─ 更新宠物表情
    ↓
5. 后续处理
   ├─ 保存对话历史到数据库
   ├─ 更新宠物状态（亲密度+1）
   ├─ 检查成就解锁
   └─ 记录统计数据
```

### 3. 宠物互动流程

```
用户点击宠物
    ↓
1. 点击检测 (PetContainer.onClick)
   ├─ 获取点击坐标
   ├─ 计算相对位置
   └─ 判断互动区域
       ├─ 头部（上 1/3）→ pet（抚摸）
       ├─ 身体（中 1/3）→ feed（喂食）
       └─ 下半身（下 1/3）→ play（玩耍）
    ↓
2. 冷却检查 (checkCooldown)
   ├─ 读取上次互动时间
   ├─ 计算时间差
   └─ 判断是否在冷却中
       ├─ 是 → 显示倒计时，终止
       └─ 否 → 继续
    ↓
3. 执行互动 (handleInteraction)
   ├─ 播放 Live2D 动画
   │   ├─ pet → tap_head
   │   ├─ feed → eat
   │   └─ play → happy
   ├─ TTS 语音反馈
   │   ├─ "好舒服~"
   │   ├─ "好好吃!"
   │   └─ "好开心!"
   ├─ 粒子效果
   │   ├─ 爱心 ❤️
   │   ├─ 星星 ⭐
   │   └─ 食物图标 🍔
   └─ 更新属性值
       ├─ pet: mood+10, intimacy+2
       ├─ feed: mood+8, energy+15, intimacy+1
       └─ play: mood+12, energy-5, intimacy+3
    ↓
4. 后续处理
   ├─ 保存状态到数据库
   ├─ 记录到 daily_stats
   ├─ 检查成就解锁
   ├─ 检查阶段升级
   │   ├─ STRANGER (0-30)
   │   ├─ FRIEND (31-70)
   │   └─ SOULMATE (71-100)
   └─ 触发表情更新
```

### 4. 属性衰减流程

```
触发时机：
├─ 应用启动
├─ 用户互动
└─ 定时检查（每 30 秒）
    ↓
1. 计算衰减 (calculateDecay)
   ├─ 读取 lastInteraction 时间戳
   ├─ 计算时间差（小时）
   ├─ 应用衰减公式
   │   ├─ mood -= hoursPassed × 2
   │   └─ energy -= hoursPassed × 1.5
   └─ 限制最小值为 0
    ↓
2. 应用衰减 (applyDecay)
   ├─ 更新 mood 值
   ├─ 更新 energy 值
   └─ 保持 intimacy 不变（只增不减）
    ↓
3. 保存状态
   ├─ 写入数据库
   └─ 更新 petStatusStore
    ↓
4. 触发 UI 更新
   ├─ 属性条动画
   ├─ 表情切换
   │   ├─ energy < 20 → sleepy
   │   ├─ mood >= 70 → happy
   │   ├─ mood >= 40 → normal
   │   └─ mood < 40 → sad
   └─ 状态提示
       ├─ mood < 20 → "我有点不开心..."
       └─ energy < 20 → "好累...想睡觉..."
```

### 5. Agent 任务执行流程

```
触发 Agent 任务
├─ 用户手动触发（对话中）
├─ 调度器触发（定时任务）
└─ 事件触发（低心情提醒等）
    ↓
1. 创建 Agent Runtime
   ├─ 加载 LLM 配置
   ├─ 加载 System Prompt
   ├─ 加载允许的工具
   └─ 设置最大步数
    ↓
2. 执行 Agent 循环
   ├─ 第 1 步：理解任务
   │   ├─ LLM 分析用户意图
   │   └─ 决定使用哪些工具
   ├─ 第 2 步：调用工具
   │   ├─ 文件工具（读写文件）
   │   ├─ 剪贴板工具（读写剪贴板）
   │   ├─ 搜索工具（网络搜索）
   │   ├─ 天气工具（查询天气）
   │   └─ 打开工具（打开 URL/应用）
   ├─ 第 3 步：处理结果
   │   ├─ 解析工具返回
   │   └─ 继续或结束
   └─ 循环直到完成或达到最大步数
    ↓
3. 返回结果
   ├─ 生成最终回复
   ├─ 显示在对话窗口
   └─ 记录到审计日志
    ↓
4. 后续处理
   ├─ 更新宠物状态
   ├─ 记录统计数据
   └─ 检查成就解锁
```

### 6. 任务调度流程

```
调度器运行中
    ↓
1. 任务注册
   ├─ 用户创建任务
   ├─ 预设任务自动注册
   └─ 保存到数据库
    ↓
2. 触发器评估
   ├─ Cron 触发器
   │   ├─ 解析 cron 表达式
   │   ├─ 计算下次运行时间
   │   └─ 到时间时触发
   ├─ Interval 触发器
   │   ├─ 计算间隔时间
   │   └─ 到时间时触发
   ├─ Event 触发器
   │   ├─ 监听事件总线
   │   └─ 事件发生时触发
   └─ Manual 触发器
       └─ 用户手动触发
    ↓
3. 动作执行
   ├─ Notification 动作
   │   ├─ 显示系统通知
   │   ├─ 显示 Toast
   │   └─ 显示宠物气泡
   ├─ Agent Task 动作
   │   ├─ 创建 Agent Runtime
   │   ├─ 执行 Agent 任务
   │   └─ 返回结果
   ├─ Workflow 动作
   │   ├─ 加载工作流
   │   ├─ 执行 LangGraph
   │   └─ 返回结果
   └─ Script 动作
       └─ 执行自定义脚本（未来功能）
    ↓
4. 记录执行
   ├─ 保存执行记录
   ├─ 更新任务统计
   └─ 触发事件通知
```

---

## 🗄️ 数据库设计

### 数据库表结构

#### 1. config 表（配置表）
```sql
CREATE TABLE config (
  id INTEGER PRIMARY KEY,
  key TEXT NOT NULL UNIQUE,
  value TEXT NOT NULL,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```
存储应用配置，包括：
- LLM 配置（provider、model、apiKey、baseUrl）
- 窗口配置（size、position、alwaysOnTop）
- 交互配置（clickThrough、rememberPosition）
- 语音配置（tts、stt、voice）
- 皮肤配置（skinId）

#### 2. pet_status 表（宠物状态表）
```sql
CREATE TABLE pet_status (
  id INTEGER PRIMARY KEY,
  mood REAL DEFAULT 80,              -- 心情值 (0-100)
  energy REAL DEFAULT 100,           -- 精力值 (0-100)
  intimacy REAL DEFAULT 0,           -- 亲密度 (0-100)
  last_interaction DATETIME,         -- 上次互动时间
  last_feed DATETIME,                -- 上次喂食时间
  last_play DATETIME,                -- 上次玩耍时间
  total_interactions INTEGER DEFAULT 0,  -- 累计互动次数
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 3. skins 表（皮肤表）
```sql
CREATE TABLE skins (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  path TEXT NOT NULL,
  preview_image TEXT,
  is_builtin INTEGER DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 4. tasks 表（任务表）
```sql
CREATE TABLE tasks (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  description TEXT,
  trigger_type TEXT NOT NULL,        -- 'cron' | 'interval' | 'event' | 'manual'
  trigger_config TEXT NOT NULL,      -- JSON
  action_type TEXT NOT NULL,         -- 'agent_task' | 'notification' | 'workflow'
  action_config TEXT NOT NULL,       -- JSON
  enabled INTEGER DEFAULT 1,
  last_run DATETIME,
  next_run DATETIME,
  metadata TEXT,                     -- JSON
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME
);
```

#### 5. task_executions 表（任务执行记录表）
```sql
CREATE TABLE task_executions (
  id TEXT PRIMARY KEY,
  task_id TEXT NOT NULL,
  status TEXT NOT NULL,              -- 'running' | 'success' | 'failed' | 'cancelled'
  started_at DATETIME NOT NULL,
  completed_at DATETIME,
  result TEXT,                       -- JSON
  error TEXT,
  duration INTEGER,                  -- 毫秒
  FOREIGN KEY (task_id) REFERENCES tasks(id)
);
```

#### 6. daily_stats 表（每日统计表）
```sql
CREATE TABLE daily_stats (
  id INTEGER PRIMARY KEY,
  date TEXT NOT NULL UNIQUE,         -- YYYY-MM-DD
  pet_count INTEGER DEFAULT 0,       -- 抚摸次数
  feed_count INTEGER DEFAULT 0,      -- 喂食次数
  play_count INTEGER DEFAULT 0,      -- 玩耍次数
  chat_count INTEGER DEFAULT 0,      -- 对话次数
  total_duration INTEGER DEFAULT 0,  -- 互动总时长（秒）
  mood_avg REAL,                     -- 当日平均心情
  energy_avg REAL,                   -- 当日平均精力
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 7. achievements 表（成就表）
```sql
CREATE TABLE achievements (
  id TEXT PRIMARY KEY,
  type TEXT NOT NULL,                -- 'interaction' | 'duration' | 'intimacy' | 'special'
  name TEXT NOT NULL,
  description TEXT,
  icon TEXT,
  unlock_condition TEXT,             -- JSON
  unlocked_at DATETIME,
  is_unlocked INTEGER DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 8. conversations 表（对话历史表）
```sql
CREATE TABLE conversations (
  id TEXT PRIMARY KEY,
  role TEXT NOT NULL,                -- 'user' | 'assistant' | 'system'
  content TEXT NOT NULL,
  timestamp DATETIME DEFAULT CURRENT_TIMESTAMP,
  metadata TEXT                      -- JSON
);
```

#### 9. agent_audit 表（Agent 审计日志表）
```sql
CREATE TABLE agent_audit (
  id TEXT PRIMARY KEY,
  source TEXT NOT NULL,              -- 'chat' | 'scheduler' | 'manual'
  prompt TEXT NOT NULL,
  tools_used TEXT,                   -- JSON array
  steps_taken INTEGER,
  result TEXT,
  error TEXT,
  started_at DATETIME NOT NULL,
  completed_at DATETIME,
  duration INTEGER                   -- 毫秒
);
```

---

## 🎨 核心功能模块

### 1. Live2D 渲染系统

**核心文件**:
- `src/services/live2d/manager.ts` - Live2D 管理器
- `src/hooks/useLive2D.ts` - Live2D Hook
- `src/components/pet/PetContainer.tsx` - 宠物容器

**功能特性**:
- ✅ 支持 Live2D Cubism 3/4 模型
- ✅ 实时表情切换（happy、sad、thinking、confused 等）
- ✅ 动画播放（tap_head、eat、happy 等）
- ✅ 自动眨眼、呼吸动画
- ✅ 鼠标追踪（眼睛跟随鼠标）
- ✅ 点击检测（头部、身体、脚部）
- ✅ 皮肤切换（支持自定义皮肤导入）

**表情映射规则**:
```typescript
function getMoodEmotion(mood: number, energy: number): EmotionType {
  if (energy < 20) return 'sleepy';  // 优先级1：疲惫
  if (mood >= 70) return 'happy';
  if (mood >= 40) return 'normal';
  if (mood >= 20) return 'sad';
  return 'confused';  // mood < 20
}
```

### 2. AI 对话系统

**核心文件**:
- `src/services/llm/chat.ts` - 对话服务
- `src/services/llm/providers.ts` - 提供商配置
- `src/hooks/useChat.ts` - 对话 Hook
- `src/components/chat/ChatWindow.tsx` - 对话窗口

**支持的 LLM 提供商**:
- **OpenAI** - GPT-4、GPT-3.5-turbo
- **Anthropic** - Claude 3.5 Sonnet、Claude 3 Opus
- **Ollama** - 本地模型（llama3、qwen 等）

**对话流程**:
1. 用户输入消息
2. 构建对话上下文（包含历史消息）
3. 注入 System Prompt（根据亲密度动态调整）
4. 调用 LLM API（流式返回）
5. 显示 AI 回复
6. TTS 语音播放（可选）
7. 保存对话历史

**System Prompt 动态调整**:
```typescript
function buildSystemPrompt(intimacy: number): string {
  const basePrompt = "你是用户的AI桌面宠物...";
  
  if (intimacy < 30) {
    // STRANGER 阶段：礼貌但疏离
    return `${basePrompt}\n你和用户刚认识，保持礼貌但不要过于亲密。回答简洁专业。`;
  } else if (intimacy < 70) {
    // FRIEND 阶段：友善活泼
    return `${basePrompt}\n你和用户已经是朋友了，可以活泼一些，偶尔分享你的'想法'。`;
  } else {
    // SOULMATE 阶段：亲密默契
    return `${basePrompt}\n你和用户已经很亲密了，你了解TA的习惯，会主动关心TA。`;
  }
}
```

### 3. Agent 工具系统

**核心文件**:
- `src/services/agent/runtime.ts` - Agent 运行时
- `src/services/agent/base-tool.ts` - 工具基类
- `src/services/agent/tools/` - 内置工具

**内置工具**:
1. **文件工具**
   - `file_read` - 读取文件内容
   - `file_write` - 写入文件内容
   - `file_exists` - 检查文件是否存在

2. **剪贴板工具**
   - `clipboard_read` - 读取剪贴板内容
   - `clipboard_write` - 写入剪贴板内容

3. **搜索工具**
   - `web_search` - 网络搜索（DuckDuckGo）

4. **天气工具**
   - `weather_query` - 查询天气信息

5. **打开工具**
   - `open_url` - 打开 URL
   - `open_app` - 打开应用程序

**Agent 执行流程**:
```
用户: "帮我搜索今天的天气"
  ↓
Agent 分析: 需要使用 weather_query 工具
  ↓
调用工具: weather_query({ city: "北京" })
  ↓
工具返回: { temperature: 15, condition: "晴" }
  ↓
Agent 回复: "今天北京天气晴，温度 15°C"
```

### 4. 任务调度系统

**核心文件**:
- `src/services/scheduler/manager.ts` - 调度管理器
- `src-tauri/src/scheduler.rs` - Rust 调度器（后端）
- `src/components/settings/SchedulerTestPanel.tsx` - 调度器测试面板

**触发器类型**:
1. **Cron 触发器** - 定时触发
   ```json
   {
     "type": "cron",
     "expression": "0 9 * * *"  // 每天 9:00
   }
   ```

2. **Interval 触发器** - 间隔触发
   ```json
   {
     "type": "interval",
     "seconds": 3600  // 每小时
   }
   ```

3. **Event 触发器** - 事件触发
   ```json
   {
     "type": "event",
     "eventName": "low_mood"  // 心情低落时
   }
   ```

4. **Manual 触发器** - 手动触发
   ```json
   {
     "type": "manual"
   }
   ```

**动作类型**:
1. **Notification 动作** - 显示通知
   ```json
   {
     "type": "notification",
     "title": "任务提醒",
     "body": "该休息了！"
   }
   ```

2. **Agent Task 动作** - 执行 Agent 任务
   ```json
   {
     "type": "agent_task",
     "prompt": "查询今天的天气并告诉我",
     "toolsAllowed": ["weather_query"],
     "maxSteps": 3
   }
   ```

3. **Workflow 动作** - 执行工作流
   ```json
   {
     "type": "workflow",
     "workflowId": "daily_summary",
     "input": {}
   }
   ```

**预设任务示例**:
- 早晨问候（每天 8:00）
- 午休提醒（每天 12:00）
- 晚安问候（每天 22:00）
- 心情低落关怀（mood < 30 时）
- 长时间未互动提醒（2 小时无互动）

### 5. 宠物养成系统

**核心文件**:
- `src/services/pet/status.ts` - 状态计算服务
- `src/services/pet/interaction.ts` - 互动处理服务
- `src/services/pet/growth.ts` - 成长阶段逻辑
- `src/stores/petStatusStore.ts` - 宠物状态 Store

**核心属性**:
1. **mood（心情值）** - 0-100
   - 影响表情选择
   - 影响对话语气
   - 每小时衰减 2 点

2. **energy（精力值）** - 0-100
   - 影响响应速度
   - energy < 20 进入睡眠状态
   - 每小时衰减 1.5 点

3. **intimacy（亲密度）** - 0-100
   - 影响 AI 人格
   - 影响功能解锁
   - 只增不减

**互动类型**:
| 互动类型 | 触发区域 | 属性效果 | 动画 | 冷却时间 |
|---------|---------|---------|------|---------|
| **pet**（抚摸） | 头部 | mood+10, intimacy+2 | tap_head | 60s |
| **feed**（喂食） | 身体 | mood+8, energy+15, intimacy+1 | eat | 120s |
| **play**（玩耍） | 脚部 | mood+12, energy-5, intimacy+3 | happy | 90s |

**成长阶段**:
| 阶段 | Intimacy 范围 | AI 人格 | 解锁功能 |
|-----|-------------|--------|---------|
| **STRANGER** | 0-30 | 礼貌但疏离 | 基础对话、基础互动 |
| **FRIEND** | 31-70 | 友善活泼 | 主动提醒、情绪感知 |
| **SOULMATE** | 71-100 | 亲密默契 | 深度对话、个性化任务 |

### 6. 语音交互系统

**核心文件**:
- `src/services/voice/manager.ts` - 语音管理器
- `src/services/voice/tts-edge.ts` - Edge TTS
- `src/services/voice/tts-web.ts` - Web Speech TTS
- `src/services/voice/stt-web.ts` - Web Speech STT
- `src/hooks/useVoice.ts` - 语音 Hook
- `src/hooks/usePushToTalk.ts` - 按键说话 Hook

**TTS（文字转语音）**:
- **Edge TTS** - 微软 Edge 语音合成（高质量）
- **Web Speech TTS** - 浏览器内置语音合成（兼容性好）

**STT（语音转文字）**:
- **Web Speech STT** - 浏览器内置语音识别

**使用方式**:
1. 点击麦克风按钮开始录音
2. 说话
3. 松开按钮停止录音
4. 自动转换为文字并填充到输入框
5. AI 回复时自动播放语音

### 7. 长期记忆系统

**核心文件**:
- `src/services/memory/long-term-memory.ts` - 长期记忆服务
- `src/services/memory/user-profile.ts` - 用户画像管理
- `src/services/memory/emotion-detector.ts` - 情绪检测

**功能特性**:
- ✅ **用户画像记录** - 自动记录用户偏好、习惯、常用表达
- ✅ **上下文记忆** - 跨会话记忆对话内容和关键信息
- ✅ **情绪检测** - 分析用户情绪状态（开心、难过、焦虑等）
- ✅ **智能回溯** - 引用历史对话内容，增强连贯性

**记忆类型**:
1. **用户偏好** - 喜好、习惯、常用词汇
   ```typescript
   {
     key: "favorite_food",
     value: "寿司",
     updatedAt: 1640000000000
   }
   ```

2. **重要事件** - 用户提到的重要日期、事件
   ```typescript
   {
     type: "event",
     content: "下周三有重要会议",
     timestamp: 1640000000000
   }
   ```

3. **情绪历史** - 用户情绪变化记录
   ```typescript
   {
     emotion: "happy",
     intensity: 0.8,
     reason: "项目顺利完成",
     timestamp: 1640000000000
   }
   ```

### 8. 主动关怀系统

**核心文件**:
- `src/stores/assistantStore.ts` - AI助手状态管理
- `src/components/settings/tabs/AssistantTab.tsx` - 助手设置面板

**功能特性**:
- ✅ **偏好记忆** - 记住用户的个性化设置和喜好
- ✅ **习惯追踪** - 学习用户的使用习惯和行为模式
- ✅ **智能提醒** - 基于习惯的主动提醒（休息、喝水、锻炼等）
- ✅ **闹钟系统** - 自定义提醒和定时任务
- ✅ **灯光控制** - 模拟环境光线（学习模式、休息模式）
- ✅ **技能建议** - 基于当前场景推荐AI技能

**关怀场景**:
1. **作息提醒**
   - 长时间工作 → 提醒休息
   - 深夜未睡 → 提醒休息
   - 早起时段 → 问候并提供天气信息

2. **健康关怀**
   - 定时提醒喝水
   - 久坐提醒活动
   - 用眼提醒（看远处放松）

3. **情绪关怀**
   - 检测到负面情绪 → 安慰和鼓励
   - 长期低落 → 推荐放松活动
   - 压力过大 → 推荐减压方法

### 9. 快捷键系统

**核心文件**:
- `src/services/keyboard/shortcuts.ts` - 快捷键管理
- `src/services/keyboard/index.ts` - 快捷键服务入口

**支持的快捷键**:
- `Cmd/Ctrl + Shift + C` - 打开聊天窗口
- `Cmd/Ctrl + Shift + S` - 打开设置窗口
- `Cmd/Ctrl + Shift + H` - 显示/隐藏宠物窗口
- `Cmd/Ctrl + Shift + Q` - 退出应用
- `Esc` - 关闭当前窗口

**特性**:
- ✅ 全局快捷键支持
- ✅ 可自定义快捷键配置
- ✅ 快捷键冲突检测
- ✅ 平台适配（macOS/Windows/Linux）

### 10. Markdown 渲染系统

**核心文件**:
- `src/components/chat/MessageItem.tsx` - 消息项组件（支持Markdown）

**功能特性**:
- ✅ **完整Markdown语法** - 支持标题、列表、代码块、表格等
- ✅ **代码高亮** - 自动识别编程语言并高亮显示
- ✅ **链接预览** - 自动识别链接并提供点击跳转
- ✅ **数学公式** - 支持LaTeX数学公式渲染（可选）
- ✅ **表情符号** - 支持emoji渲染

**支持的语法**:
```markdown
# 标题
**粗体** *斜体* ~~删除线~~
- 列表项
1. 有序列表
[链接](https://example.com)
`代码`
```javascript
// 代码块
console.log('Hello');
```
```

### 11. 数据管理系统

**核心文件**:
- `src/services/data/export.ts` - 数据导出
- `src/services/data/import.ts` - 数据导入
- `src/services/data/backup.ts` - 数据备份/恢复
- `src/services/data/validators.ts` - 数据验证
- `src/components/settings/DataSettings.tsx` - 数据管理面板

**功能特性**:
1. **数据导出**
   - 导出对话历史（JSON格式）
   - 导出配置设置
   - 导出皮肤数据
   - 导出Agent角色和MCP服务器配置
   - 支持选择性导出

2. **数据导入**
   - 导入JSON格式数据
   - 数据验证和去重
   - 冲突处理策略（覆盖/跳过/合并）
   - 导入进度提示

3. **数据备份**
   - 完整数据库备份（SQLite文件）
   - 自动备份（按设定周期）
   - 备份管理（查看、恢复、删除）
   - 备份元数据（时间、大小、描述）

4. **数据恢复**
   - 从备份恢复数据
   - 恢复前预览
   - 增量恢复支持

**导出数据结构**:
```json
{
  "version": "1.0",
  "exportedAt": "2025-12-29T12:00:00Z",
  "data": {
    "conversations": [...],
    "config": {...},
    "skins": [...],
    "agent_roles": [...],
    "mcp_servers": [...]
  }
}
```

### 12. 统计与成就系统

**核心文件**:
- `src/services/statistics/index.ts` - 统计服务
- `src/services/achievements/index.ts` - 成就服务
- `src/services/database/statistics.ts` - 统计数据库操作
- `src/services/database/achievements.ts` - 成就数据库操作

**统计维度**:
- **每日统计** - 各类互动次数、互动时长、平均属性值
- **累计统计** - 总陪伴天数、总互动次数、连续互动天数
- **趋势分析** - 近 7 天互动曲线、属性变化趋势

**成就类别**:
1. **互动成就** - 累计互动次数
   - 初次见面（1 次）
   - 熟悉的朋友（100 次）
   - 形影不离（1000 次）

2. **时长成就** - 陪伴天数
   - 一周之约（7 天）
   - 月度伙伴（30 天）
   - 百日陪伴（100 天）

3. **亲密度成就** - 成长阶段
   - 成为朋友（intimacy 31）
   - 亲密伙伴（intimacy 71）

4. **特殊成就** - 特殊条件
   - 夜猫子（凌晨 2-4 点互动 10 次）
   - 早起鸟（早上 6-8 点互动 10 次）

---

## 🔧 开发指南

### 环境要求

- **Node.js**: >= 18.0.0
- **pnpm**: >= 8.0.0
- **Rust**: >= 1.70.0
- **Tauri CLI**: >= 2.0.0

### 安装依赖

```bash
# 安装前端依赖
pnpm install

# 安装 Tauri CLI（如果未安装）
pnpm add -D @tauri-apps/cli
```

### 开发命令

```bash
# 启动前端开发服务器（热更新）
pnpm dev

# 启动 Tauri 开发窗口（前端 + 原生端）
pnpm dev:tauri

# 构建前端静态文件
pnpm build

# 打包桌面应用
pnpm build:tauri

# 预览构建结果
pnpm preview
```

### 工具脚本

```bash
# 检查 Live2D 配置
pnpm check:live2d

# 启用 Live2D
pnpm enable:live2d

# 重置配置
pnpm reset:config
```

### 代码规范

#### TypeScript
- 严格模式，禁止使用 `any`
- 函数组件优先，使用 Hooks 管理副作用
- 单个组件不超过 400 行，复杂组件需拆分
- 文件名使用 kebab-case，组件名使用 PascalCase
- Hook 命名使用 `useXxx` 格式

#### 样式
- 优先使用 Tailwind CSS
- 避免自定义 CSS，统一放在 `src/styles/`
- 使用设计令牌（design-tokens.css）

#### 状态管理
- 使用 Zustand 管理全局状态
- 状态集中放入 `src/stores/`
- 跨层数据使用 Zod 校验

#### 提交规范
- 使用约定式提交前缀：`feat`/`fix`/`chore`/`docs`/`refactor`
- 示例：`feat: add live2d toggle`

---

## 📊 性能优化

### 1. Live2D 渲染优化
- 空闲时降低帧率（60fps → 15fps）
- 互动时恢复高帧率
- 使用 PixiJS 的硬件加速

### 2. 属性计算优化
- 使用时间差计算而非定时器轮询
- 属性变化 < 5 时不触发 UI 更新
- 使用缓存避免重复计算

### 3. 数据库查询优化
- 使用索引（date、task_id 等）
- 批量写入互动记录（每 5 分钟一次）
- 使用事务保证数据一致性

### 4. 内存管理
- 对话历史限制最近 100 条
- 定期清理过期的执行记录
- 使用 Immer 优化状态更新

---

## 🚀 部署与打包

### 打包桌面应用

```bash
# 打包所有平台
pnpm build:tauri

# 打包特定平台
pnpm tauri build --target x86_64-apple-darwin  # macOS
pnpm tauri build --target x86_64-pc-windows-msvc  # Windows
pnpm tauri build --target x86_64-unknown-linux-gnu  # Linux
```

### 打包产物

```
src-tauri/target/release/bundle/
├── macos/
│   └── AI Desktop Pet.app
├── windows/
│   ├── AI Desktop Pet.exe
│   └── AI Desktop Pet.msi
└── linux/
    ├── ai-desktop-pet.AppImage
    └── ai-desktop-pet.deb
```

### 配置签名（macOS）

```json
// src-tauri/tauri.conf.json
{
  "bundle": {
    "macOS": {
      "signingIdentity": "Developer ID Application: Your Name (TEAM_ID)",
      "providerShortName": "TEAM_ID"
    }
  }
}
```

---

## 🔐 安全与隐私

### API 密钥管理
- API 密钥存储在本地 SQLite 数据库
- 不上传到服务器
- 使用 Tauri 的安全存储机制

### 数据隐私
- 所有对话历史存储在本地
- 不收集用户数据
- 不上传到云端

### 权限管理
- 文件操作需要用户授权
- 剪贴板访问需要用户授权
- 网络请求仅用于 AI API 调用

---

## 🐛 已知问题与限制

### 1. Live2D 兼容性
- 部分旧版 Live2D 模型可能不兼容
- 需要手动调整动画映射表

### 2. 语音识别
- Web Speech API 依赖浏览器支持
- 部分浏览器可能不支持

### 3. 调度器精度
- Cron 表达式精度为分钟级
- 不支持秒级精度

### 4. Agent 工具限制
- 文件操作仅限用户目录
- 网络请求可能被防火墙拦截

---

## 🗺️ 未来规划

### Phase 1: MVP 扩展（✅ 已完成）
- ✅ 基础属性系统（mood、energy、intimacy）
- ✅ 点击互动系统（pet、feed、play）
- ✅ 心情驱动表情切换
- ✅ Live2D 动画渲染
- ✅ AI 智能对话（多LLM支持）
- ✅ 语音交互（TTS/STT）

### Phase 2: 增强留存（✅ 已完成）
- ✅ 成长阶段系统（STRANGER、FRIEND、SOULMATE）
- ✅ 智能提醒任务
- ✅ 每日互动统计
- ✅ 成就系统
- ✅ Agent 工具系统
- ✅ 任务调度系统
- ✅ 数据导入导出

### Phase 3: 智能化增强（✅ 已完成）
- ✅ 长期记忆系统（用户画像、情绪检测）
- ✅ 主动关怀系统（习惯追踪、智能提醒）
- ✅ Markdown 渲染支持
- ✅ 快捷键系统
- ✅ 游戏化 UI 体系（Radix Toast、马卡龙设计）
- ✅ 表情包系统（多风格支持）

### Phase 4: 差异化功能（🔄 进行中）
- 🔄 AI 协作小任务（文件管理、日程安排等）
- 🔄 多模态交互（图片识别、手写识别）
- 🔲 节日/季节主题皮肤
- 🔲 多宠物系统（宠物互动、群养）
- 🔲 社区分享功能（皮肤、设置、Agent角色）

### Phase 5: 生态扩展（📋 规划中）
- 📋 插件市场（社区开发的Agent工具、皮肤）
- 📋 云同步功能（跨设备同步数据）
- 📋 移动端伴侣应用
- 📋 桌面小部件集成
- 📋 开放API（第三方集成）

---

## 📚 参考资料

### 技术文档
- [Tauri 官方文档](https://tauri.app/)
- [React 官方文档](https://react.dev/)
- [Zustand 文档](https://zustand-demo.pmnd.rs/)
- [LangGraph 文档](https://langchain-ai.github.io/langgraph/)
- [Live2D Cubism SDK](https://www.live2d.com/sdk/)

### 设计参考
- QQ 宠物玩法分析
- Tamagotchi 养成机制
- Animal Crossing 社交系统

---

## 👥 贡献指南

### 如何贡献

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'feat: add some amazing feature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

### PR 规范

- PR 描述需包含：变更摘要、影响范围、验证方式
- 界面改动需附前后对比截图
- 依赖升级需说明动机与回滚方案

---

## 📄 许可证

本项目采用 MIT 许可证。详见 [LICENSE](LICENSE) 文件。

---

## 📧 联系方式

如有问题或建议，欢迎通过以下方式联系：

- GitHub Issues: [提交 Issue](https://github.com/your-repo/ai-desktop-pet/issues)
- Email: your-email@example.com

---

**最后更新**: 2025-12-29

---

## 📝 更新日志

### 2025-12-29
- ✨ 新增长期记忆系统（用户画像、情绪检测、上下文记忆）
- ✨ 新增主动关怀系统（偏好记忆、习惯追踪、智能提醒）
- ✨ 新增快捷键系统（全局快捷键支持）
- ✨ 新增Markdown渲染支持（代码高亮、表格、公式）
- ✨ 新增数据管理系统（导入/导出/备份/恢复）
- 🎨 引入游戏化UI体系（Radix Toast、马卡龙设计）
- 🎨 新增表情包系统（多风格表情反馈）
- 📝 更新项目说明文档，补充所有新功能介绍

### 2025-12-28
- ✨ 完成Phase 1/2全功能落地
- 📚 建立完整文档体系
